local BattleBalanceConfig = {}

BattleBalanceConfig.Defaults = {
    AbilityTickIntervalSeconds = 0.08,
    AbilityFirstCastDelaySeconds = 2.0,
    AbilityMinCooldownSeconds = 1.0,
}

BattleBalanceConfig.Pve = {
    AbilityTickIntervalSeconds = 0.08,
    AbilityFirstCastDelaySeconds = 2.0,
}

BattleBalanceConfig.Pvp = {
    AbilityTickIntervalSeconds = 0.08,
    AbilityFirstCastDelaySeconds = 2.0,
}

return BattleBalanceConfig
