-- StyleConfig.luau
-- Centralized style/theme configuration for UI

return {
    Colors = {
        Primary = Color3.fromRGB(52, 92, 158),
        Secondary = Color3.fromRGB(44, 126, 72),
        Accent = Color3.fromRGB(132, 96, 42),
        Background = Color3.fromRGB(24, 24, 24),
        Text = Color3.new(1, 1, 1),
    },
    Fonts = {
        Main = Enum.Font.SciFi,
        Title = Enum.Font.SourceSansBold,
        Stat = Enum.Font.Arcade,
    },
    Sizes = {
        ToggleButton = UDim2.fromOffset(108, 36),
        Modal = UDim2.fromOffset(760, 320),
        TouchTargetMin = 44,  -- Apple HIG minimum for mobile; use DeviceContext.getTouchTargetMin() at runtime
    },
    -- Floating combat damage text (DamagePopupManager + DamageNumberView). MapleStory-ish: big, readable, long float; crits pop in hot yellow/orange vs normal red/pink.
    DamageNumbers = {
        Default = {
            Size = UDim2.fromOffset(140, 40),
            StartPosition = UDim2.fromScale(0.5, 0.42),
            EndPosition = UDim2.fromScale(0.5, 0.1),
            DurationSeconds = 1.9,
            RiseDurationSeconds = 0.45,
            FadeDurationSeconds = 2.2,
            VerticalStackSpacingPx = 16,
            Prefix = "-",
            Font = Enum.Font.LuckiestGuy,
            TextSize = 30,
            TextColor = Color3.fromRGB(255, 150, 130),
            TextStrokeColor = Color3.fromRGB(18, 10, 10),
            TextStrokeTransparency = 0,
            ZIndex = 122,
        },
        -- Normal hits: coral / soft red (not yellow — so crit yellow reads clearly).
        PlayerToNpc = {
            Size = UDim2.fromOffset(148, 42),
            StartPosition = UDim2.fromScale(0.5, 0.38),
            EndPosition = UDim2.fromScale(0.5, 0.08),
            RootStartOffsetY = -18,
            RootEndOffsetY = -100,
            DurationSeconds = 2.1,
            RiseDurationSeconds = 0.4,
            FadeDurationSeconds = 1.7,
            VerticalStackSpacingPx = 18,
            Prefix = "-",
            Font = Enum.Font.LuckiestGuy,
            TextSize = 30,
            TextColor = Color3.fromRGB(255, 165, 150),
            TextStrokeColor = Color3.fromRGB(20, 12, 12),
            TextStrokeTransparency = 0.02,
            ZIndex = 125,
        },
        NpcToPlayer = {
            Size = UDim2.fromOffset(140, 40),
            StartPosition = UDim2.fromScale(0.5, 0.42),
            EndPosition = UDim2.fromScale(0.5, 0.1),
            RootStartOffsetY = -16,
            RootEndOffsetY = -96,
            DurationSeconds = 1.7,
            RiseDurationSeconds = 0.32,
            FadeDurationSeconds = 1.35,
            VerticalStackSpacingPx = 16,
            Prefix = "-",
            Font = Enum.Font.LuckiestGuy,
            TextSize = 30,
            TextColor = Color3.fromRGB(255, 130, 130),
            TextStrokeColor = Color3.fromRGB(14, 8, 8),
            TextStrokeTransparency = 0,
            ZIndex = 122,
        },
        PvpHit = {
            Size = UDim2.fromOffset(132, 38),
            StartPosition = UDim2.fromScale(0.5, 0.42),
            EndPosition = UDim2.fromScale(0.5, 0.1),
            DurationSeconds = 1.9,
            RiseDurationSeconds = 0.34,
            FadeDurationSeconds = 1.56,
            VerticalStackSpacingPx = 16,
            Prefix = "-",
            Font = Enum.Font.LuckiestGuy,
            TextSize = 30,
            TextColor = Color3.fromRGB(255, 155, 140),
            TextStrokeColor = Color3.fromRGB(16, 10, 10),
            TextStrokeTransparency = 0,
            ZIndex = 122,
        },
    },
    -- Merged onto base row by DamagePopupManager. Crit = vivid yellow/orange + bigger + longer; normal stays base red tones.
    DamageCategoryOverrides = {
        NormalDamage = {},
        CritDamage = {
            TextSizeAdd = 15,
            TextColor = Color3.fromRGB(255, 255, 95),
            TextStrokeColor = Color3.fromRGB(120, 40, 8),
            ScalePunch = 1.26,
            DurationSeconds = 2.2,
            RiseDurationSeconds = 0.36,
            FadeDurationSeconds = 1.84,
        },
        Dodge = {
            OverrideText = "MISS",
            TextSize = 30,
            TextColor = Color3.fromRGB(170, 185, 230),
            TextStrokeColor = Color3.fromRGB(22, 24, 45),
            DurationSeconds = 1.9,
            RiseDurationSeconds = 0.34,
            FadeDurationSeconds = 1.56,
        },
        Miss = {
            OverrideText = "MISS",
            TextSize = 30,
            TextColor = Color3.fromRGB(160, 165, 190),
            DurationSeconds = 1.9,
            RiseDurationSeconds = 0.34,
            FadeDurationSeconds = 1.56,
        },
        ShieldAbsorb = {
            Prefix = "«",
            TextSize = 30,
            TextColor = Color3.fromRGB(140, 215, 255),
            TextStrokeColor = Color3.fromRGB(10, 28, 45),
            DurationSeconds = 1.95,
            RiseDurationSeconds = 0.34,
            FadeDurationSeconds = 1.61,
        },
        DoTDamage = {
            TextSizeAdd = 0,
            TextColor = Color3.fromRGB(255, 120, 200),
            DurationSeconds = 1.95,
            RiseDurationSeconds = 0.34,
            FadeDurationSeconds = 1.61,
        },
        Heal = {
            Prefix = "+",
            TextSize = 30,
            TextColor = Color3.fromRGB(110, 255, 150),
            TextStrokeColor = Color3.fromRGB(8, 45, 22),
            DurationSeconds = 2.05,
            RiseDurationSeconds = 0.36,
            FadeDurationSeconds = 1.69,
        },
    },
}